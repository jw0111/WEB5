import React from 'react';

const intro = ({history}) => {
      return (
        <div>
            <header>
                <h1>사이트 소개</h1>
            </header>
        </div>
      );
  }

export default intro;