import React from 'react';

const home = ({history}) => {
      return (
        <div>
            <header>
                <h1>메인페이지</h1>
            </header>
        </div>
      );
  }

export default home;